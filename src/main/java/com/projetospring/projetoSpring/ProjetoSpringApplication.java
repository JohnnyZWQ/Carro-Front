package com.projetospring.projetoSpring;
import org.springframework.boot.SpringApplication; 
import org.springframework.boot.autoconfigure.SpringBootApplication; 

@SpringBootApplication
public class ProjetoSpringApplication {

    /*
     * Método principal (main), ponto de entrada da aplicação.
     * Aqui, o Spring Boot inicializa toda a aplicação.
     */
    public static void main(String[] args) {
        SpringApplication.run(ProjetoSpringApplication.class, args); // Inicia a aplicação Spring Boot.
    }
}
